[project]
name = "data-maturity-compass"
version = "0.1.0"
description = "Data Maturity Compass (Databricks) - assessment de maturidade e diagnostico direcionado a Databricks"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "psycopg[binary,pool]>=3.2.0",
    "databricks-sdk>=0.81.0",
    "pydantic>=2.0.0",
    "python-multipart>=0.0.9",
]

[tool.uv]
package = false
