# Pin the npm registry to the public registry so that `npm install` never
# writes a Databricks-internal proxy URL (npm-proxy.dev.databricks.com) into
# package-lock.json — even when run from inside the Databricks network.
# See issue #6.
registry=https://registry.npmjs.org/
